import { LampDemo } from '../Components/ui/lamp'; // Ajusta la ruta según la ubicación real de LampDemo

function Layout() {
  return (
    <div>
      <LampDemo />
    </div>
  );
}

export default Layout;
